# Particles module

Owns the CPU particle pool, particle lifecycle and CPU-path shaders. Public
entry point: `core/particles/particle_system.h`. GPU compute particles remain
in the separate `compute/` backend.
